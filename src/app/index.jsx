import React from "react";
import ReactDOM from "react-dom";
import App from "./page";
import "./index.css"; // Para estilos opcionales

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
